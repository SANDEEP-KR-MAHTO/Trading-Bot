python-binance==1.0.19        # Binance API client library
python-dotenv==1.0.1          # Load API keys from .env file
requests==2.32.3              # HTTP requests (dependency of python-binance)
websockets==12.0              # Required by python-binance for streams
pandas>=2.0,<3.0                # For data manipulation if extended later
reportlab==4.2.2              # For PDF generation (used in report)
common
streamlit
